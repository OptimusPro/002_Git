# This is a cool proyect

My project is very cool

## Setup

You can start the project with the start command

### Building step

1. Install nodejs: `npm i -g node`
2. Install npx: `npm i npx`
3. Install dependencies: `npm intall`